<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/headerAdmin.ejs") %>
<body class="bg-gray-100 flex items-center justify-center h-screen">

  <form action="/admin/login" method="POST" class="bg-white p-6 rounded shadow-md w-96 space-y-4">
    <h2 class="text-xl font-semibold text-center">Admin Login</h2>

    <% if (error) { %>
      <div class="text-red-500 text-sm"><%= error %></div>
    <% } %>

    <input type="text" name="username" placeholder="Username" required class="w-full border p-2 rounded" />
    <input type="password" name="password" placeholder="Password" required class="w-full border p-2 rounded" />

    <button type="submit" class="bg-blue-600 text-white py-2 px-4 rounded w-full">Login</button>
  </form>
</body>
</html>
